/*
function()
function is used to write the most reusable code, function should have a
specific name to declare and call.function is very useful to prevent from 
write same code again and again
*/

function demo(){
	if (3<5) {
		document.write("3 is smaller then 5");
	}
}

demo();